# Git
.git
.gitignore
.github

# Dev artifacts
node_modules
.next
*.log
npm-debug.log*

# Test / lint
coverage
.nyc_output
vitest.config.*

# Temporary game source clones
_tmp_sf*
_tmp_slug*

# Unity source (already built externally if needed)
# games-src/ is intentionally NOT ignored — Pygbag needs it

# Editor / OS
.vscode
.idea
*.DS_Store
Thumbs.db

# Env files (injected via docker-compose env)
.env.local
.env*.local

# Misc
README*.md
AGENTS.md
*.zip
!public/games/slug/mslug.zip
!public/games/sidekicks/ssideki.zip
!public/games/bios/*.zip
